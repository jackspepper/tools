% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_clariostar_csv.R
\name{read_clariostar_csv}
\alias{read_clariostar_csv}
\title{Read a BMG CLARIOstar CSV export}
\usage{
read_clariostar_csv(path_csv)
}
\arguments{
\item{path_csv}{Path to the \code{.csv} file exported from the CLARIOstar
control software.}
}
\value{
A list with elements:
\describe{
\item{metadata}{Named list of run metadata parsed from the export
header (User, Path, Test run no., Test name, Date, Time, ID1, ...).}
\item{data}{Tidy long-format data frame, sourced from the
\code{"Well,Content,..."} table when present, otherwise from the
parsed measure matrices.}
\item{matrix_data}{Tidy long-format data frame parsed from the
stacked measure matrices, or \code{NULL} if none are found.}
\item{format_used}{Character; \code{"tidy"} or \code{"matrix"}, indicating
which section became \code{data}.}
\item{parse_info}{Named list of parse-time tracking metadata: the
source filename, package version, and parse timestamp.}
}
}
\description{
Parses a CLARIOstar plate reader CSV export into a tidy long-format
data frame plus parsed run metadata. This is a separate entry point
from \code{\link[=read_clariostar]{read_clariostar()}} because the CSV export packages both the
stacked measure matrices (like the Excel \code{"Microplate End point"}
sheet) and the tidy per-well table (like the Excel \code{"Table All Data points"} sheet) into a single ragged, comma-delimited file, along with
instrument settings and a run log that this function ignores.
}
\examples{
\dontrun{
result <- read_clariostar_csv("ClarioSTAR_Export.csv")
result$metadata
result$data
result$parse_info
}

}
