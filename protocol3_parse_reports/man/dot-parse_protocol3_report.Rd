% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_helpers.R
\name{.parse_protocol3_report}
\alias{.parse_protocol3_report}
\title{Internal: shared report parser runner}
\usage{
.parse_protocol3_report(
  file,
  sheet,
  name_components,
  data_cols,
  num_cols,
  split_flags,
  exclude,
  exclude_column,
  include_version = TRUE
)
}
\arguments{
\item{file}{Path to the .ods report file.}

\item{sheet}{Sheet name or index to read (default: 1, the first sheet).}

\item{name_components}{Optional character vector naming the underscore-
separated components of "Plate Name" (e.g.
c("Date", "BacTime", "Subject", "PlateID")). If supplied, these become
additional columns in \code{metadata}. Plate names with a different number of
underscore-separated parts than \code{length(name_components)} are left as NA
with a warning.}

\item{data_cols}{Character vector of data column names in file order.}

\item{num_cols}{Character vector of column names to convert to numeric.}

\item{split_flags}{Logical. If \code{TRUE} (default), an additional
\code{Flags_list} column is added to each plate's \code{data}, holding the
individual flag tokens as a character vector per row (e.g. \code{"E M"}
becomes \code{c("E", "M")}).}

\item{exclude}{Optional character string. Plates whose \code{exclude_column}
value contains this string are removed from the returned list and
reported to the console (e.g. \code{exclude = "_Exclude"}). Default \code{NULL}
(no filtering).}

\item{exclude_column}{Which metadata field to search for \code{exclude}.
Default \code{"Plate Name"}.}

\item{include_version}{Logical. If \code{TRUE} (default), adds a \code{"Parser Version"}
column (e.g. \code{"0.1.0.9000"}) to each plate's \code{metadata} data frame and
attaches \code{"parser_version"} and \code{"parsed_at"} attributes to the returned list.}
}
\value{
Parsed plates list, or list with \verb{$included} and \verb{$excluded}.
}
\description{
Handles common workflow for parsing blocks, post-processing plate data,
and applying exclusions.
}
\keyword{internal}
