% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_flags.R
\name{split_flags}
\alias{split_flags}
\title{Split a ProtoCOL 3 "Flags" string into a vector}
\usage{
split_flags(flags)
}
\arguments{
\item{flags}{Character vector (typically a \code{Flags} column).}
}
\value{
A list the same length as \code{flags}, each element a character vector
of the individual flag tokens for that row (\code{character(0)} for NA/blank).
}
\description{
ProtoCOL 3 stores multiple flags as a single space-separated string in the
\code{Flags} column (e.g. \code{"E M"}). This splits that string into a character
vector (e.g. \code{c("E", "M")}) so flags can be tested/filtered individually.
}
\examples{
split_flags(c("E M", "M", NA))

}
