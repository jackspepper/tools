% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_protocol3.R
\name{.apply_exclusion}
\alias{.apply_exclusion}
\title{Apply an exclusion filter to a list of parsed plates}
\usage{
.apply_exclusion(plates, exclude, column)
}
\arguments{
\item{plates}{A named list as returned by \code{\link[=parse_protocol3]{parse_protocol3()}} or
\code{\link[=parse_protocol3_abx]{parse_protocol3_abx()}}.}

\item{exclude}{Character string to match against \code{column} (fixed
substring match, not regex). If \code{NULL} (default), no filtering is done
and everything is returned under \verb{$included}.}

\item{column}{Which field to search for \code{exclude}. One of \code{"Plate Name"},
\code{"User"}, \code{"Comments / Notes"}, or any other metadata/name_components
column present in each plate's \verb{$metadata}. Default \code{"Plate Name"}.}
}
\value{
A list with \verb{$included} and \verb{$excluded}, each a named list of
plates in the same shape as \code{plates}.
}
\description{
Splits a parsed plates list into plates whose \code{column} value matches
\code{exclude} and those that don't. Matches are printed to the console.
}
\keyword{internal}
