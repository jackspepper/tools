% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_protocol3.R
\name{parse_protocol3_abx}
\alias{parse_protocol3_abx}
\title{Parse ProtoCOL 3 Antibiotic Resistance (Zone) Report}
\usage{
parse_protocol3_abx(
  file,
  sheet = 1,
  name_components = NULL,
  split_flags = TRUE,
  exclude = NULL,
  exclude_column = "Plate Name"
)
}
\arguments{
\item{file}{Path to the .ods report file.}

\item{sheet}{Sheet name or index to read (default: 1, the first sheet).}

\item{name_components}{Optional character vector naming the underscore-
separated components of "Plate Name" (e.g.
c("Date", "BacTime", "Subject", "PlateID")). If supplied, these become
additional columns in \code{metadata}. Plate names with a different number of
underscore-separated parts than \code{length(name_components)} are left as NA
with a warning.}

\item{split_flags}{Logical. If \code{TRUE} (default), an additional
\code{Flags_list} column is added to each plate's \code{data}, holding the
individual flag tokens as a character vector per row (e.g. \code{"E M"}
becomes \code{c("E", "M")}).}

\item{exclude}{Optional character string. Plates whose \code{exclude_column}
value contains this string are removed from the returned list and
reported to the console (e.g. \code{exclude = "_Exclude"}). Default \code{NULL}
(no filtering).}

\item{exclude_column}{Which metadata field to search for \code{exclude}.
Default \code{"Plate Name"}.}
}
\value{
If \code{exclude} is \code{NULL}, a named list, one element per plate (named
after "Plate Name"), each a list with \verb{$metadata} and \verb{$data} data
frames. If \code{exclude} is supplied, a list with \verb{$included} and
\verb{$excluded}, each in that same per-plate shape.
}
\description{
Reads a ProtoCOL 3 antibiotic zone plate report (.ods) and splits it into
one entry per imaged plate, mirroring \code{\link[=parse_protocol3]{parse_protocol3()}} for colony
counts. Each entry contains \code{metadata} (Plate Name, User, Created,
Comments / Notes) and \code{data} (Sector, Zone Name, Zone Diameter (mm),
Antibiotic Susceptibility, Flags).
}
\examples{
file <- system.file(
  "extdata",
  "ExampleAbxRes.ods",
  package = "protocol3Parser"
)

plates <- parse_protocol3_abx(file)
plates[["1.1A"]]$data
plates[["1.1B"]]$data$Flags_list

# Drop any plate whose name contains "_Exclude"
res <- parse_protocol3_abx(file, exclude = "_Exclude")
names(res$included)
names(res$excluded)

}
