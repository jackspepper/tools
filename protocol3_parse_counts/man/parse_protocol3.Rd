% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_protocol3.R
\name{parse_protocol3}
\alias{parse_protocol3}
\title{Parse ProtoCOL 3 Colony Counter Report}
\usage{
parse_protocol3(file, sheet = 1, name_components = NULL)
}
\arguments{
\item{file}{Path to the .ods report file.}

\item{sheet}{Sheet name or index to read (default: 1, the first sheet).}

\item{name_components}{Optional character vector naming the underscore-
separated components of "Plate Name" (e.g.
c("Date", "BacTime", "Subject", "PlateID")). If supplied, these become
additional columns in \code{metadata}. Plate names with a different number of
underscore-separated parts than \code{length(name_components)} are left as NA
with a warning.}
}
\value{
A named list, one element per plate (named after "Plate Name").
Each element is a list with \verb{$metadata} and \verb{$data} data frames.
}
\description{
Reads a ProtoCOL 3 .ods report and splits it into one entry per imaged
plate. Each entry contains two tables: \code{metadata} (Plate Name, User,
Created, Comments / Notes) and \code{data} (Sector, Colony Name, Count / Frame,
Flags).
}
\examples{
file <- system.file(
  "extdata",
  "ExampleCounts.ods",
  package = "protocol3Parser"
)

plates <- parse_protocol3(file)
plates[["Plate1A"]]$metadata
plates[["Plate1A"]]$data

# Split "Plate1A" into named components
plates <- parse_protocol3(
  file,
  name_components = c("Date", "BacTime", "Subject", "PlateID")
)
plates[["Plate1A"]]$metadata

}
