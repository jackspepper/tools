% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_plate_path.R
\name{example_plate_path}
\alias{example_plate_path}
\title{Path to packaged example qPCR data}
\usage{
example_plate_path(dir_only = FALSE)
}
\arguments{
\item{dir_only}{Logical. If \code{FALSE} (default), returns the full
path to \code{examplePlate.csv}. If \code{TRUE}, returns the path to the
package's \code{extdata} directory containing the example file.}
}
\value{
A character string containing either:
\itemize{
\item the full path to \code{examplePlate.csv} when
\code{dir_only = FALSE}
\item the path to the package \code{extdata} directory when
\code{dir_only = TRUE}
}
}
\description{
Returns the path to the example qPCR plate export included with
the package. By default, the full path to \code{examplePlate.csv}
is returned. Optionally, the path to the containing \code{extdata}
directory can be returned for functions that expect a folder
rather than a single file.

The example data can be used with
\code{\link[=run_cleaning_pipeline]{run_cleaning_pipeline()}} and
\code{\link[=run_consolidation_pipeline]{run_consolidation_pipeline()}} to demonstrate or test the
qPCR processing workflow.
}
\examples{
# Get the example plate file
plate_file <- example_plate_path()

# Get the directory containing the example plate
plate_dir <- example_plate_path(dir_only = TRUE)

# Run the cleaning pipeline using the example directory
result <- run_cleaning_pipeline(
  plate_dir,
  dry_run = TRUE
)

}
